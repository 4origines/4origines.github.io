---
layout: base
icon: fa-utensils
title: Menu
eleventyNavigation:
  key: Menu
  order: 1
---

<p class="alert alert-danger" role="alert">
  The menu is just an example of that the menu will look like.
</p>


<div id="menu" class="row g-5">
  {% for item in menu.en %}
  <section class="col-6">
    <div class="p-3 border">
      <h2 class="">{{ item.section }}</h2>
      <p>{{ item.description }}</p>
      {% for item in item.items %}
      <section class="mt-3">
        <h3>
          {{ item.name }}
          <small style="font-weight:300; font-size: 1rem">
            {% if item.Vegetarian == true %} - Vegetarian{% endif %}
            {% if item.Vegan == true %} - Vegan{% endif %}
            {% if item.GlutenFree == true %} - Gluten Free{% endif %}
          </small>
        </h3>
          <p>{{ item.description }}</p>
          {# <p>${{ item.price }}</p> #}
    </section>
    {% endfor %}
    </div>
  </section>
  {% endfor %}
</div>

